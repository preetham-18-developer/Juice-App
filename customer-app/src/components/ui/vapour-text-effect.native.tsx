import React from 'react';

/**
 * Native-safe version of VaporizeTextCycle / IntroComponent.
 * Returns null to avoid web-only API errors (canvas, etc.).
 */
export const VaporizeTextCycle = () => null;
export const IntroComponent = () => null;

export default IntroComponent;
